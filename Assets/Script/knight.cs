using UnityEngine;

public class knight : Enemy
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public bool flag = false;

    public override void Serang()
    {
        Debug.Log("KNIGHT SEARARARNGNGG");
    }
}
